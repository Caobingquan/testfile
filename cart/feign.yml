server:
  port: 8030
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: feign
  thymeleaf:
    prefix: classpath:/static/
    suffix: .html
  mvc:
    static-path-pattern: /static/**
